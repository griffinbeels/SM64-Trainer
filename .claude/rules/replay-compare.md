---
paths:
  - "src/sm64_events/replay/**"
  - "src/sm64_events/compare/**"
  - "src/sm64_events/core/recorder_lock.py"
  - "src/sm64_events/ui/components/replay.js"
  - "src/sm64_events/ui/components/compare.js"
  - "src/sm64_events/ui/components/videosync.js"
  - "src/sm64_events/ui/components/failcomp.js"
  - "src/sm64_events/ui/frame.js"
---

# Replay, compare, compilation — where to change what

| To change... | Edit |
|---|---|
| Replay orchestration (attach loop, source wiring, ring, idle gate) | `replay/recorder.py` + player-input tap `replay/activity.py`; `replay/clock.py` is THE QPC↔UTC contract. Routes BOTH streams into the single AV sink (`_on_frame`→`submit`, `_on_pcm`→`submit_audio`); the in-process `SegmentWriter` is a no-ffmpeg fallback only. Idle THROTTLES capture grabs (`is_idle` → `video.set_idle_check`) AND discards segments; the ring byte-cap is free-disk-gated (`ring.effective_cap`) |
| Video capture (DWM surface primary; GDI fallback) | `replay/video.py` + `replay/_dwm.py` — docstrings carry the PJ64 capture pathology and the no-user32-on-grab-thread rule; `grab_period` trickles grabs to 8 Hz while idle (kills the ~2 GB/s frame-alloc churn; ffmpeg feeder untouched so resume stays seamless); active capture oversamples (fps×2 DWM / fps×1.5 GDI) — a system-lag suspect, see docs/architecture.md. PJ64 window locate in `replay/window.py` (`find_window`/`pick_window`/`WindowInfo`, wired via `main.py` `window_finder=`) |
| WHEN each game frame happened (the clip's frame map) | `replay/frameclock.py`, THREE series, per-slot rank presents > feeds > edges. The PRESENT series (v4, item 30): the poller watches PJ64's host-side present counter (hunted per session by `memory/present.py`; the poller's `_watch_presents` marks each tick with the frame read on the same poll tick, skipping the unobserved first) and `derive_present_frames` turns ticks into frames — within a run frame = count + shift (shift from the run's delta FLOOR − `PRESENT_LAG_FRAMES`), so the logic→present wobble lands in the tick TIMES; a re-present train (logic frozen, counter ticking) derives nothing (timer-span guard + tail-trim, both mutation-proved). The FEED series (v2): `recorder._on_frame` tags each captured picture at capture with `capture_tag(utc_of(ts_100ns))` — the RAM frame AND the picture's own composition time; the tag rides `FfmpegAvSink.submit` to the feeder and `mark_feed` records what was fed; the presents lookup keys on that composition time. The EDGE series (v1): the poller marks every frame edge's wall time (250 Hz). v2 exists because edges + ANY constant lag measured ±1 two-sided on clip 741 (8/44 exact); v4 exists because v2 still held the present wobble (24/53 exact on the same scorer). Two measured constants live in frameclock.py and are re-measured by the A-icon template method, never guessed: `MAP_WALL_BIAS_S` (the encode chain answers half a slot late, uniformly -- attempt 2147: feeds 10/10 at -1 slot) shifts every slot's lookup wall, and each run's PHASE is normalized against the poller's edge series (each hunted counter ticks at its own pipeline point; two sessions measured 17 ms apart), with `PRESENT_TICK_TRAIL_S` only the no-edges fallback. **THEN the map is held to ONE ANSWER PER PICTURE and checked against the clip's own pixels** (`replay/mapalign.py`). `picture_runs` finds each distinct picture in the footage (160x120 grey, mean-diff >= 0.35) and `quantised` gives every video frame of a run the same answer -- his ruling 2026-08-28, "if there's duplicated frames, input timeline should be identical for the sequential duplicated frames", measured on his clip 4374: 257 pictures carried two different frames before it and 0 after. TWO rules, and the second was found by his stepping: every video frame of one picture answers the same, AND **consecutive PICTURES are consecutive FRAMES** -- holding the boundaries alone left the map's own irregular advance untouched (his clip 4441: 230 pictures sharing a frame with the next and 225 skipping one; three different pictures all read frame 7, then a jump to 9). Each picture is pushed to the nearest series rising by >=1 per picture (`_rising`, pool-adjacent-violators on value-minus-index) -- a least-squares fit, NOT a walk that bumps each past its predecessor, because a dip is evidence about all its neighbours and bumping would drag every later picture up to satisfy one bad reading (mutation-proved by exactly that case). After it: 0 shared, 1,157 of 1,163 advancing by one, clip span unchanged. Rising by MORE survives where the map says so, since the capture really does miss frames. The runs supply the BOUNDARIES, the map still supplies the ADVANCE (numbering pictures consecutively would drift by every game frame the capture missed -- 428 pictures for ~478 frames on that clip). It runs even when the aligner refuses, because it needs nothing but the pictures. **THEN the map is checked against the clip's own pixels and shifted onto them** (`replay/mapalign.py`, 2026-08-28): the constants above estimate a journey (logic -> present -> capture -> encoded slot) that RAM cannot measure, and four rounds of setting them by hand each landed somewhere else (-2 slots, +1, -3, every one verified live). Usamune's stick digits are IN the footage and the input track says what they should read, so `align_clip` fits per-glyph ink weights at every candidate offset and the best fit names the error; `ReplayService._align_to_the_footage` applies it. The verdict gate judges the peak against the best offset OUTSIDE its own +-1 neighbourhood (`gate`), because on a quantised map the neighbour is near-equivalent by construction -- the neighbour-inclusive gate refused 4441's clean peak on a 0.007 tie and the clip ran a frame ahead (2026-08-29, pinned on that clip's real curve). The correction lands in the FRAME domain (`frame_corrected`, round(slots/2) on every value) so an odd slot shift can never re-split pictures. A clip whose digits cannot be read inherits the MEDIAN anchor other clips measured (`AnchorStats`, data/mapalign_anchor.json, min 3 measurements, newest 40 kept) -- calibrate-once made continuous; `frame_map_learned` marks it. It REFUSES (display off, too little track, no clear winner) rather than shift by a fitted nothing -- the sidecar's `frame_map_aligned`/`frame_map_offset`/`frame_map_fit` say which happened, and the aligner is injected from `main.py` because it needs the input track. Same code as `tools/score_frame_map.py`'s verdict, so the tool cannot drift from what extraction did. `ReplayService.view()` writes the sidecar's `frame_map` + `frame_map_source` ("presents"/"feeds"/"edges"/"mixed") at extraction; a save keeps both; `tools/score_frame_map.py` prints the source with its verdict. Never absorb a per-clip desync into a new constant — score it with the tool; the offset is only the fallback for pre-map clips |
| THE PAD READER -- the map READ off Usamune's input display, and the per-clip 100% meter (2026-09-01, his ruling: "anything less than 100% accuracy is failure") | `replay/padread.py`. Every timing route (edges, feeds, presents, the ledger) inherits the logic->picture lag, which WANDERS by whole frames inside one clip (5431: +5 to -1 in 24 s), and the ink anchor above weighs one number per slot -- too weak to pin a picture (the refit it fed broke clip 5165). So the display is read as GROUND TRUTH, because he defined it that way: six glyph cells per picture (letter/d1/d2 on the U-D line and the L-R line, `REGION` + `ROWS`/`COLS` as fractions of the frame, cells scaled to 105x88), one template per painted GLYPH (fifteen classes, not one per value -- the per-field fingerprint in `pixelmap.py` needed hundreds and read 84%/49%), weighted to the pixels the font paints (`ink_mask`) where the exemplars agree (median + MAD, so occluded sightings cannot poison it); a cell reads only by a clear margin (`DIST_MAX`/`MARGIN_MIN`) else it is UNKNOWN -- there is no blank class, so an empty cell can never be read as a glyph. `align` is a monotone DP over raw frames within `BAND` of the prior map: one unit per known cell a candidate frame contradicts, `HOLE_COST` on uncaptured frames, physics from the picture flags (a slot inside one picture may only dwell; a NEW picture pays `DWELL_ACROSS_COST` to stay), the prior's own STEPS preferred where the pictures say nothing (`STEP_TETHER` -- the clocks know where capture dropped a frame even when their absolute answer has wandered), a tiny position tether as tie-break. `read_clip` runs two passes: the shipped reference alphabet (`data/pad_glyphs_us.npz`, learned off 7 min of his ring footage 2026-09-01 and verified against the track) reads first so a prior frames out cannot mislead the first templates, the path labels every frame, the clip re-learns its own glyphs at its own scale. MEASURED on that footage (18,411 frames, the title screen's scrolling 'USAMUNE ROM' overlay as an adversarial occluder): 13,524 of 13,525 fully-read slots agree with the aligned frame, ZERO reads name a value the track never held, and a prior deliberately +5/0/-2 by thirds with 5% holes is pulled back onto the display everywhere it reads. Wired in `main.py` as `replay.pad_reader` (needs the input track over the clip's whole span -- `track_with_lead`); `ReplayService._read_the_display` runs it after the quantiser and BEFORE the ink anchor, which stands down when it answers; the sidecar carries `frame_map_read` + `pad_reading` (sure/agree/nowhere/known_cells/disagreements). THE METER: `uv run python tools/score_pad_read.py --attempt N [--disagreements]` -- SURE / AGREE / NOWHERE / MOVED and every contradicted slot. THE RESET'S WHITE FLASH IS AN ANCHOR (2026-09-01, his frames 0-7 on 5534): the first white picture of a reset IS the spawn frame the journal recorded (`flash_anchors`, `ANCHOR_COST`; `main.py` passes the attempt's own anchor when it is a practice_reset), measured there with his C-down on the first faded-in picture confirming the count -- the one anchor a reset's washed-out, resting neighbourhood offers. BUTTON ICONS are a LIVE second channel (2026-09-01): per-button templates learned like the digits (`learn_icons`, first position, single-button frames, gated on 24 sightings over 3 presses AND a saturated fill -- the grey Z's white core alone matched sand on 452 slots), a shipped reference (`data/pad_glyphs`' sibling `pad_icons_us.npz`, learned off 5574 + 5782, keyed by button NAME -- a bit-keyed loader had swallowed a ValueError and the channel never ran until the fresh-context review's T6 caught it), `read_icons` at every packed position, and `align` pays one unit per lit icon the candidate frame does not hold. MEASURED: A reads 345/345 on 5782 and 596/598 on 5574; B/C/Z wait for footage with enough presses (the reference regenerates from `tools/`-less scratch today -- owed a tool). THE METER IS HONEST NOW, AND WHY IT WAS NOT (fresh-context review, 2026-09-01, T1): `score` counted the objective `align` minimised, so a map corrupted by +2 across 120 slots inside a hold returned a bit-identical verdict. It now reports, in the timeline's unit, `frames_checked` of `frames_total` (checkable = the magnitude digit read; blank cells pass), the disagreements, and `unpinned_longest` -- the longest run of frames the display cannot tell apart (inside a hold every neighbour reads the same; 65 frames = 2.2 s on 5782). The ceiling the review measured: the digits distinguish only ~17% of frames from a neighbour (83% sit in holds), so the reader is an OFFSET auditor and the reset flash / icon edges are the per-frame anchors; the per-frame TRUTH needs capture to encode one frame per distinct picture stamped with its RAM frame (item 38's approved recorder change, still unbuilt). HONEST LIMITS: a stretch where the display is unreadable (off, covered, a fade) or the stick rests keeps the clocks' answer for the FRAME (the pad shown is still right at rest -- any resting frame is '0'), so Mario's rows there are only as good as the prior; the letter cell is learned per direction (U silver, D/R blue, L gold) and a clip with a digit the reference lacks (x/d1 '9') teaches it from its own holds or leaves those cells unknown |
| THE CAPTURE LAYER (round 32 item 95, 2026-09-04): recording frames that ARE game frames | `plugin/gfxwrap/gfxwrap.c` is a wrapper graphics plugin for Project64 1.6 (Zilmar 1.3; `zilmar.h` holds the spec, `stream.h` the shared-memory layout): it forwards everything to the user's own plugin (`wrapped=` in `sm64_trainer_gfx.ini`), copies the tracker's ADDRESS TABLE out of RDRAM at ProcessDList (that list IS frame N: gGlobalTimer still reads N, the pad is N's) and, at the VI whose VI_ORIGIN changed, reads GL_FRONT itself when the emulation thread holds a context, else through the wrapped plugin's own ReadScreen -- GLideN64_LINK_4.2 renders on a thread of its own (measured 2026-09-05 through the host driving the real DLL), so for him it is always ReadScreen: a command that render thread runs after the swap, its buffer freed through HeapFree once HeapValidate says the static CRT put it on the process heap (STATUS_READSCREEN in the header says so) -- publishing (picture, stamp) into THE FRAME STREAM (`replay/framestream.py`, one mapping + one event, a 3-slot ring). The plugin knows NO game address -- the table comes from the live layout (`replay/pluginsource.py::table_for`), so `memory/layout.py` stays the one source. `PluginVideoSource` decodes a slot's bytes with the sampler's own `inputs/frame.py::decode` over a `RegionMemory` (PJ64 word order, the same swap rules as `memory/base.py`) and calls `recorder._on_frame(bgra, ts_100ns, stamp)`; the recorder tags the picture with the STAMP's frame (the frame clock is not asked) and the ledger row says `exact`. `ReplayService._map_from_feeds` joins such rows to the clip's pictures with lag 0 and `None` on an inexact row (2026-09-05: the desktop lag had put every plugin picture one frame behind its own stamp), and `_take_the_stamps`: every row exact -> the map IS the rows (`frame_map_source: plugin`), `picture_igt` = the stamped IGT per slot (the inspector's clock), the aligner / timer join / reader alignment stand down, the reader audits, and `_audit_pad_stamps` compares the stamp's pad with the input track's (`pad_stamp_agreement`, no pixels). `main.py` opens the stream at boot, writes the table, and picks the plugin source per attach when the heartbeat moves within 0.2 s AND `pluginsource.pictures_flow` sees a first picture within 0.6 s -- otherwise `DesktopUntilLayerPresents`: the desktop grab watching the heartbeat once a second, which ends itself like a lost window the moment a picture flows so the attach loop's next factory call gets the plugin (the recorder attaches to PJ64's window before the ROM runs, and the order he opens things in must not matter -- 2026-09-05), with the reason on `/api/replay/status` `frame_source_note` while a loaded layer refuses (asked again every 15 s; a layer whose heartbeat moved while it refused every picture held an empty ring for an hour). `CaptureLayer.refresh_if_stale` copies a newer build's DLL over the installed one at boot while PJ64 is closed; the setup screen's row shows its problems and an Update button otherwise. Build: `tools/build_plugin.py` (x86 MSVC, /MT; the DLL is COMMITTED under `data/plugin/`); test without PJ64: `tests/test_gfxwrap_host.py` drives `plugin/gfxwrap/host.c` (a tool window + GL context, fake RDRAM, a fake wrapped plugin painting the colour the host wrote) and pins the C offsets against Python's. THE ORACLE (`replay/oracleread.py`, `tools/score_oracle.py --attempt N`): the frame number Usamune's HUD memory display of 0x8032D5D4 prints into the picture, read map-free through the +1 rule (a value counts only when a neighbour vouches for it; near-identical duplicate pictures allow 0; a digit-count guard drops truncated reads; bridged predictions are verified box by box; the reference alphabet `data/oracle_glyphs_us.npz` was learned off his three oracle clips 6650/6702/6678, kept under `replays/oracle/`). It certifies ANY map -- on those clips the timer map scored 553/601, 154/288 and 216/229 exact. Install/undo: `core/capturelayer.py` (registry `HKCU\Software\N64 Emulation\Project64 Version 1.6\Dll` `Graphics Dll`, the Plugin folder, refuses while PJ64 runs because 1.6 rewrites the registry on exit); the consent UI is the setup screen (`ui/components/setupmodal.js`, `server/setup_api.py`) |
| THE CLOCK READER -- primary frame identity on newly stamped clips (round 32 item 91) | `replay/timerread.py` + `inputs/sampler.py`/`replay/frameclock.py`/`replay/recorder.py` stamps. A displayed CLOCK value exactly names a frame only inside its own IGT epoch; it does **not** prove five-glyph OCR and does not contain an absolute `gGlobalTimer` epoch. Capture therefore records coherent `(G, I) = (gGlobalTimer, usamune_overall)` beside each picture and the reader computes `shown = ceil(cs*3/10)`, `lag = I-shown`, `displayed frame = G-lag` per picture. No controller data or prior-map constant enters a mechanical slot. Necessary guards: legal final digit, lag 0..12 (narrower than a one-second glyph error; the measured healthy path is single digits), and bounded monotone transitions. An isolated rejected slot plus frozen/unreadable/unpaired slots are filled by a nearest-anchor shift of the prior and counted as `bridged`; >3 consecutive rejected slots refuses the entire timer path because that means the counter-domain/OCR premise failed, not that a long stretch was measured. Older address/layout comments incorrectly said `usamune_overall` survived subarea loads; the newer measured `counter_epoch.py` contract proves it is leg-local, so the join is local and fail-closed instead of fitting one run-wide epoch. `ReplayService` runs this before the pad reader; the pad reader's pre-alignment verdict audits it and cannot mutate it. Sidecar/API: `frame_map_source: timer`, `frame_map_mode: timer|timer+bridge`, `timer_reading` counts + observed lag range, `frame_map_inferred` for bridges. `frame_map_degraded` remains capture health and is not overloaded. Older clips with no IGT stamp retain the prior pad/map path. |
| THE PICTURE FEED (item 38, built 2026-09-02): ONE video frame per distinct captured picture, at its own time, audio on the same clock -- the frame map becomes bookkeeping | `replay/config.py::picture_feed` (the one switch; False restores the CFR feed); `replay/ffmpeg_sink.py` (`_picture_feed_loop` writes each queued picture once and a heartbeat repeat after `PICTURE_HEARTBEAT_S`; `_mux_picture`/`_audio_mux_loop` mux rawvideo + pcm_s16le into a NUT stream over stdin with THIS process's stamps in `PICTURE_TIME_BASE`, seconds since the run's spawn; `-fps_mode passthrough -enc_time_base demux`, `-reset_timestamps 0`, no `+nobuffer`; `on_fed` files every write); `replay/recorder.py` (feeds only a grab the ledger calls a NEW picture, tag = the row's ts; wires `on_fed` to the ledger); `replay/ledger.py` (`mark_fed`/`feeds_between`, the feed log); `replay/feedmap.py` (`feed_map`: frame k at start + frame_times[k] -> the feed entry by wall time, bias measured per clip, one entry answers one frame, repeats inherit, stats); `replay/extract.py` (`frame_times_of`, the passthrough cut, `ClipResult.frame_times`); `replay/service.py::_map_from_feeds` (sidecar `frame_times`, `encode: picture_feed`, `feed_match`, `repeats`; source `feed_log`; the reader gets `repeats=` as its picture flags). THREE MEASUREMENTS decided the shape, each the obvious version failing (sink docstring, scratch probes 2026-09-02): ffmpeg's read-time stamps bunch a whole segment one 90 kHz tick apart because its scheduler reads a pipe in bursts; a wall-clock-stamped audio pipe beside a stamped video stream throttled video to 26 of 30 pictures/s at every offset tried; `+nobuffer` discards the packets stream analysis reads (23 of the first 83). PROOF, real ffmpeg: `tests/test_replay_picture_feed.py` -- sink -> ring -> cut -> map, 151 of 151 pictures, 0 unmatched, residual 0.15 ms median / 2 ms max, ~33 ms gaps; and the A/V instrument this pipeline never had: a flash and a click at one wall instant land together in the cut within 50 ms. The browser counts slots through `ui/frame.js::clipClock` (frame_times when present, the grid from video_start_s otherwise); `tools/probe_clip_seek.py` seeks the same way. Lost at each spawn: the pictures queued while ffmpeg starts (PICTURE_QUEUE = 16, ~0.4 s, counted as drops), never silently |
| The PICTURE LEDGER (item 40 — capture's own per-picture record, the map's FIRST source) | `replay/ledger.py` — his spec 2026-08-28: "at every single frame of gameplay we have access to all the memory addresses and data in-game… embed each frame with extra information… extend the frame-by-frame information storage at any point". `recorder._on_frame` passes every grab to `PictureLedger.observe`, which dedups by EXACT equality of a stride-8 sample (pre-encode grabs of one presented picture are byte-identical; 93 µs/grab measured at 1600×1224, ~1.1% of a core at 120/s) and records one row per DISTINCT picture: composition time, RAM frame, plus every registered stamp (`ledger.stamps["name"] = callable` — ONE line extends the record). At extraction `ReplayService._map_from_ledger` asks `rows_between`, `mapalign.ledger_map` matches the clip's picture runs to rows by time (per-clip MEDIAN of nearest deltas self-calibrates the composition-vs-slot clock bias — never a tuned constant; a run with no row inside half a picture period takes the consecutive default; a whole-period bias aliases to a frame shift the digit anchor closes), the shared rising fit paints, and the sidecar stores `frame_map_source: "ledger"` + the clip's row slice as `picture_ledger` (stamps included — the durable record for ANY future analysis). Refusal (< 3 matched runs or < half) or any exception → the frameclock series answer as before; observe() never raises, so a ledger bug cannot cost capture. Ledger-built maps are one-answer-per-picture by construction — the quantiser skips them (`frame_map_quantised` set at build). Since 2026-08-28 every row also carries `phase` (seconds from the last frame edge to the composition, `FrameClock.edge_phase`): the stamp sits 0..+2 frames from the DISPLAYED picture and that wander flips where the present drifts across a frame edge, so phase is the physical variable that predicts it — record-only until a clean-ledger clip confirms the correlation. Torn grabs (a mid-update surface copy; 118 of 647 row gaps under 20 ms on attempt 4518) fold into their present row via `MIN_ROW_GAP_S`. **Debugging ANY timeline-vs-video report starts with `tools/inspect_timeline.py`** (item 44): `--frames` prints, per panel frame, the track pads, the map's slots and the ledger rows with their stamps; `--audit` walks the whole clip (row rate, torn pairs, stamp-vs-map residual stretches). The panel's FRAME readout is 0-based = the track axis |
| The WINDOWED anchor (items 41-43 — the per-picture path over shelves and blips) | `mapalign.measure_windows` + `window_corrected`, driven by `windowed_alignment` (which the service's aligner closure calls; a bare `Alignment` return still works). The pipeline lag drifts by whole frames WITHIN a clip (4518: the digits name +0 for 0-9.5s and −2 slots after), so after the global gate passes, every encoded PICTURE chooses its offset by best path: evidence = capped squared ink residual under fixed global weights (cap `EVIDENCE_CAP`; a picture votes only when its best offset fits within `VOTE_FLOOR` noise-floors — uncapped noise CHASES, merely-below-cap noise random-walks, both measured), switching costs `VITERBI_SWITCH` noise-floors per frame scaled by WHERE (`SWITCH_AT_IRREGULAR` at a picture boundary whose map advance is not +1 — the only physically expressible seams — vs `SWITCH_AT_REGULAR` elsewhere; pictures come from `picture_runs`, never map-value runs, because a DUP is precisely what a value walk hides). `window_corrected` applies per picture and DEFERS a downward step to the map's next skip (a previous+1 clamp would cascade). All knobs dimensionless multiples of the clip's own noise floor, plateau-swept. Sidecar: `frame_map_windows` = [lo, hi, offset, fit, margin] per stretch. **THE DIGIT REFIT IS BUILT AND NOT WIRED (item 57, 2026-08-31).** `mapalign.digit_fitted`/`fit_pictures_to_frames` assign every PICTURE its own frame by ink, monotonically. It fixed one of his clips and broke the next one: where a picture cannot answer — a white fade-in, a dark room — every path costs about the same, so the search buys a rounding of fit with six-frame skips (four of them on clip 5165, 447 distinct frames against the ledger's 487, so ~60 frames had no slot and the inputs he pressed after spawning were nowhere). A step penalty AND a gate requiring it to beat the prior were both added and the wrong path still won — the finding is about the SIGNAL, not the search: total ink is one number per slot and cannot overrule the ledger. Wire it only when the digits are READ (`replay/pixelmap.py`), gated on that reader's own 99%. HONEST LIMIT: a ±1 wander confined to a gentle-stick stretch (weak digits, no irregular boundary) stays — his panels 30-43 on 4518; the phase stamp is the recorded variable expected to close it. **Round 50 additions after the three-clip verdict (island PERFECT / pyramid + lava wrong):** `unwrapped_display` turns (stamp, phase) into a per-row display estimate by unwrapping the smooth present-pipeline delay — canonical (median implied delay folded into one period) so its one remaining constant means the same thing on every clip; `ledger_map` prefers it over the stamp-minus-constant rule, and INJECTS rows no run matched (his pyramid's dark corridor merged three near-identical pictures into one run and frames 276-7 vanished — the pixels may refine boundaries, never delete a picture; injection gated to runs ≥ 2 pictures long, since jitter makes tie-losing rows look orphaned and splitting an honest run shreds the map). Path hardening, each measured on those clips: STAY on cost ties (argmin's first-index habit walked −8..+8 across the evidence-free post-grab region), and a picture votes only when its slots actually show ink (`MIN_VOTE_INK` — a menu or star dance otherwise "votes" for whichever offset predicts the least ink). Torn fold now requires the SAME stamp — a close row with a different stamp is a stall's catch-up present, a real picture. The anchor-store file was wiped 2026-08-28 (its rows predated the canonical domain); it re-learns as digit-strong clips are cut |
| The PIXEL-READ map (v3, built, NOT wired) | `replay/pixelmap.py` — reads Usamune's own display out of the footage (value-field fingerprints, weighted templates learned from the v2 prior's holds) and fits the map to the reads with a banded monotone DP; `InputsService.pad_lookup` is its data door and `tools/probe_pixel_read.py` its gate. Wire it into `ReplayService.view()` ONLY when the probe's training accuracy reads ≥99% (it read 84%/49% Y/X on clip 778, 2026-08-23 — the READER is the bottleneck, and at that accuracy a fit can move slots wrongly, which violates the 100% ruling harder than v2's honest ±1). Two dead ends recorded in the module docstring: nearest-change snapping (no-op under dense stick changes) and button-icon offset curves (anchors cannot see between themselves; grass reads as the B icon). 'Values the track never held: 0' is the measured proof 100% stays reachable |
| A+V single mux (ffmpeg subprocess) | `replay/ffmpeg_sink.py::FfmpegAvSink` — ONE ffmpeg muxes video (stdin) + audio (named pipe) on ONE wall-clock: `-use_wallclock_as_timestamps` before each input, `-fps_mode cfr` video, `-af aresample=async=1` audio → combined A+V MPEG-TS segments. THIS is the two-clock-drift fix (memory `replay-av-drift-two-clocks`); feeder pacing is not load-bearing. Segment→UTC = anchor-at-first-frame + CSV offset RELATIVE to the first segment. In-process fallback (no ffmpeg, legacy two-clock): `replay/encoder.py`. Encode QUALITY comes from `replay/config.py::video_quality_args`, not here |
| GC policy (stop-the-world watchdog + gen-2 idle collector) | `replay/_gcwatch.py` — freezes the startup heap, disables AUTO gen-2, runs manual `gc.collect(2)` during idle with a 5-min never-idle backstop; `arm(is_idle=recorder.is_idle)` from `server/app.py` lifespan |
| Audio (per-process tap primary, endpoint-by-pid fallback, RT-safe pump, deaf-stream watchdog) | `replay/audio.py` + `replay/_system_audio.py` — `ProcessAudioSource` (proctap / WASAPI **process** loopback) is PRIMARY so a clip carries the game and nothing else on the desktop; `SystemAudioSource` (device loopback of the endpoint hosting the pid's session) is the FALLBACK and records everything sharing that endpoint. Both factories take the **pid** (`recorder.py`), both feed the same `AudioPump` — a PURE RT-safe handoff (PCM → `recorder._on_pcm` → sink's audio pipe; no sample-count placement / silence injection, ffmpeg's wallclock+aresample own timing) — and both self-heal through the shared `DeafStreamWatchdog`. proctap yields float32; int16 is the one format on the wire to the sink. `proctap` must stay in `tools/build_exe.py::COLLECT` or the frozen exe silently degrades to desktop-wide capture. **`tools/probe_audio_isolation.py` is the live gate**: two ffplay children playing 440/880 Hz at once, only the 440 process captured — one tone proves the tap is alive, never that it is isolated (device loopback passes a one-tone test perfectly). Rejected alternatives and the deaf-tap escalation policy: docs/architecture.md audio facts |
| Clip extraction (ffmpeg cut of A+V segments) | `replay/extract.py` — pure ffmpeg cut: `concat:` the covering segments, accurate-seek the span, re-encode video (0.5s GOP) + faststart, `-c:a copy`. Coverage holes clamp to the contiguous run containing the span start (`contiguous_run` / `_joinable` — a FRAME-SIZE change breaks a run too). Segments carry their `dims` (`SegmentInfo`, stamped per ffmpeg child). Quality from `replay/config.py::video_quality_args` — the cut must be TRANSPARENT w.r.t. its segments |
| Encode quality (ring sink + clip cut) | `replay/config.py` — `VIDEO_CQ`/`VIDEO_CRF` + `video_quality_args(codec, stage, maxrate)`, THE registry both encoders read. Constant-quality, never a bare bitrate (the "blurry recording" bug: extractor with no rate control fell back to ffmpeg's ~2 Mbps default, fixed 2026-07-23). Raising quality costs ring disk — `VIDEO_CQ` is the one knob |
| Clip save/extract/settings orchestration | `replay/service.py` — `ReplayService`: attempt→span→clip→save + user settings + `available_attempt_ids()`; `_coverage_notices` answers whether the clip is missing any of the ATTEMPT (`starts_mid_attempt` / `ends_early`, from the timestamps) as opposed to merely a short run-up -- `replay.js` drew its "Starts mid-attempt" warning off the coarse `truncated` flag until 2026-08-28, which put an alarm on an ordinary reset ("but... I just reset as normal? What does this even mean?"); `truncated` still ships and still means what it meant; same LookupError/ValueError/RuntimeError→HTTP taxonomy as api.py |
| Single-RECORDER guard (machine-wide) | `core/recorder_lock.py` — fixed temp-dir file lock so only ONE instance captures PJ64, even across exe + dev servers in separate worktrees (different data dirs → per-db instance_lock does NOT coordinate them; two recorders doubled GPU/encode/audio load, live 2026-06-15). `recorder.py` acquires in `_begin_capture` (can't → viewer-only, auto-takeover next attach cycle), releases in `_teardown_capture`. Injectable (`recorder_lock_factory`); tests redirect the path via conftest |
| Side-by-side compare (import jobs, CRUD, view, serve) | `compare/service.py` — composes `compare/importer.py` (yt-dlp/copy/upload-bytes → ffmpeg-normalize → content-addressed cache in `core/paths.compare_cache_dir()`, dedup = "load once"; atomic publish via `.tmp-<name>`+os.replace) + `storage/db.py` comparisons (migration v10). **Wired in main.py on ffmpeg alone, never gated on the db** (task 0053): a boot that loses the instance-lock race (the reload handoff — the old app's replay teardown can outlive the new boot's bounded wait) used to skip the router entirely, so every compare call 404'd and the tab read as "videos don't load" with nothing in any log; the service reads `tracker.db` per call, answers 503 until the reattach loop lands the db, then works with no restart (pinned by `tests/test_composition.py::test_dbless_boot_still_mounts_compare_and_compilation`, which covers compilation's identical gate). The UI half of the same incident: `compare.js` refetches the view on tab activation (a fetch that failed during a restart otherwise stuck all session — re-picking the same run is a state no-op) and every import/view failure now renders (`cmpError` / job error state), never a silent swallow |
| Failure compilation (build + job) | `replay/compilation.py` — `CompilationBuilder` reuses `ClipExtractor` per window then ONE concat-filter ffmpeg pass scales+pads to a common canvas (probed from the finale clip) and re-encodes to one MP4 (single clip → copy); `CompilationService` runs it as a polled daemon-thread job (mirrors `CompareService`), output → `compilations_dir()` |

## Recipe: add a user-visible replay setting

Bounds row in `SETTINGS_LIMITS` + plumb `validate_settings`/`save_settings`/
`apply_settings_file` (replay/config.py) → live-apply + getter in
`ReplayService.update_settings`/`settings()` → field on `SettingsBody`
(server/replay_api.py) → input in the recording-dot panel
(`ui/components/replay.js` BufferSettings) → README settings lines → tests in
test_replay_{config,service,api}.py. Mirror commits 69bb83d / 29fd542.
Settings persist in `data/replay_settings.json` (a JSON overlay beats a db
migration for scalars); corrupt/out-of-range files lose to defaults so the
server always starts.


## Replay / compare / compilation UI

Moved here from `.claude/rules/ui.md` on 2026-07-28 so one zone has one rule
file across its server and UI halves. `.claude/rules/ui-core.md` also loads for
these components and carries the UI verification norms — read it before
changing a component.

| To change... | Edit |
|---|---|
| Replay player + recording dot | `ui/components/replay.js` (incl. BufferSettings panel) |
| Compare tab UI | `ui/components/compare.js` (my-run vs comparison, one centered transport; the × on a saved comparison forks on WHO added it — a user-added video is DELETED via `DELETE /api/compare/videos/{id}`, the system-provided rank-standard example is only hidden with the opt-out persisted, and the button's title says which — his call 2026-08-14, pinned by `tests/test_ui_compare_close.py`) + `ui/components/videosync.js` (`useSyncController` drives N `<video>` in lockstep; `VideoStage` forwards its element via `onEl`; `WorkArea` in/out handles + click/drag-to-scrub playhead) + `ui/frame.js` (shared game-frame step/jump, also used by replay.js) |
| Failure compilation UI | `ui/components/failcomp.js` — shared `FailureCompilation({identity})` (X/Y inputs in localStorage, Generate → poll → summary + Reveal); mounted in BOTH practice cards' detail drawer (`practice.js`), pinned by test_ui_section_parity.py |
