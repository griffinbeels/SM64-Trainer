---
paths:
  - "src/sm64_events/ui/**"
  - "src/sm64_events/links.py"
  - "tests/test_ui_section_parity.py"
  - "tests/test_star_icons.py"
---

# UI layer — where to change what

Built-in viewer: `ui/index.html` — served per request: edit + refresh, no
restart. Components/store/API client: `ui/components/` · `ui/store.js` ·
`ui/api.js` · `ui/app.js`; vendored Preact in `ui/vendor/`. Shared formatting:
`ui/format.js` (fmtIgt mirrors core/timefmt.py). Design system: one big CSS
block in index.html (cosmic "observatory": navy+gold, card-based, OBS-stable
fixed-height slots); design contract/anti-slop rules in `.agents/skills/sm64-uiux/`.

| To change... | Edit |
|---|---|
| Segments builder UI | `ui/components/segments.js` — 100% vocab-driven (labels, sentence templates, level/area/course/star enums): adding a trigger type in tracking/segments.py appears in the UI with zero JS changes. Editing an EXISTING segment offers an Icon row (live preview + the shared `iconpicker.js` modal) writing the same `/api/icon` override the banner's ✎ uses. `flow`-annotated level/subarea params (level_enter/level_exit) filter each side's dropdown to world-possible moves via vocab `connections` (`allowedIds`: dest = source's successors, source = dest's predecessors); the CURRENT value always stays listed so out-of-topology stored defs never blank; a consistency sweep in setParam clears siblings the topology rules out |
| Stage quick-select banner | `ui/components/stagebanner.js` — dispatches on `stage.mode`: STAR target in a main course; SEGMENT target in a Castle Inside subarea (segments whose start triggers begin there — `segment_targets`, derived in views.py via `_segment_start_areas`; only subarea-scoped triggers, so LBLJ never shows upstairs); a BowserCourseRow in BitDW/BitFS/BitS offering **reds** vs **no reds** (pipe segment, `_segment_start_levels`) with MUTUAL EXCLUSION and AUTO-RESTORE of that level's last pick (memory DERIVED from the pipe segment's `enabled` flag — per-level, restart-proof, can't disagree with what's tracking); an ArenaRow (Bowser 1/2/3) AUTO-selects the single fight segment on entry. Each star button shows its active-strat name + a rank Medal (`rank_by_star`, graded server-side via `_strat_rank`); rendered atop `ui/components/practice.js`. Main-course STAR mode = single non-wrapping line of SM64 stars — per-slot PNG art `ui/assets/star_{1..6}.png` (slot clamped; 100-coin/7th slot reuses star_6), rank Medal between star and name ("–" when unranked), bobbing in sync when active; look flags (`STAR_IMG_COUNT`/`STAR_DIM_IDLE`) are call-site constants. The row NEVER scrolls horizontally: cells shrinkable, art/type clamp on `cqw` against `.practice-page` (`container-type: inline-size`), `--selector-height` shrinks with pane width (width-driven only — OBS rule), strat sub-line hides under ~600px (spec 2026-07-24, pinned by tests/test_star_icons.py). Settings-drawer "Star icons" pref (`t.starIcons`, localStorage `sm64.starIcons`, DEFAULT `course`, control in header.js) swaps art to per-star split icons `ui/assets/star_icons/{prefix}{slot+1}.png` via `COURSE_ICON_PREFIXES` (course_id 1..15; 7th slot = real 100-coin icon, no clamp) — same dim/glow/bob classes, `courseicon` adds rounded corners, `onerror` falls back to the generic star. EVERY banner mode renders the shared `PracticeCell` (art / rank medal / name / sub-line — spec 2026-07-24-segment-icon-cells): segment rows use `.starrow.segcells` (pinned to the 7-column star-row width), show the segment's active strat + server-graded rank (views.py `segment_targets` carries `strat`/`rank` through the SAME `_strat_rank` path), swap the sub-line for the "⏱ running" chip while armed (`.starcell.armed`); segment art resolves user override > `LEVEL_ICONS` start-level match (17/19/21 + arenas → bitdw/bitfs/bits) > generic star. Per-entity icon OVERRIDES (win in either mode; hover ✎ → `iconpicker.js` modal, also in the segment editor) are server state: ui_state KV `icon_overrides` written by `TrackerService.set_icon` via `POST /api/icon` (stem validated against `GET /api/icons` — bundled ∪ uploaded `user:<file>`; "+" tile raw-body-uploads via `POST /api/icons/upload` into `core/paths.user_icons_dir()` = `data/icons/`, update-proof, served by `GET /api/icons/file/{name}`; `iconSrcFromStem` in iconpicker.js is THE stem→URL rule), broadcast-only `icons_changed`. ARMED VISIBILITY (user rule 2026-07-24: a running segment is never invisible): every row appends `armedExtraCells` for armed segments its filter missed, `ArmedOnlyRow` replaces the placeholder when armed with no stage row, and views.py `segment_targets` includes EVERY def (location-less starts get empty areas/levels) so the union can always find them |
| Selector star art | `ui/assets/star_{1..6}.png` — 256×256 PNGs (~7.5% transparent margin baked in; `object-fit: contain`), bundled into the exe via the `ui/` tree in build_exe.py. `ui/assets/star_icons/` — community split-icon set (credit in `assets/credit.md`): opaque 100×100 PNGs `{prefix}{1..7}.png` for all 15 main courses (+ unused specials); asset↔registry coverage pinned by tests/test_star_icons.py |
| Entity pickers (level / course / star / segment) | `ui/components/picker.js` (`GroupedPicker` — renders one `<optgroup>` per group and forwards `name` to the `<select>` for a caller that needs form-field identity, defaulting to a stable fallback so every call site gets one with zero call-site edits, but owns no domain vocabulary itself) + `ui/entities.js` (pure builders: `levelOptions`/`courseOptions`/`starOptionsFromVocab`/`starOptionsFromCatalog`/`segmentOptions`, ids are STRINGS and a star's is composite `"8:1"`; also `visibleGroups` — DROPS a group the filter emptied, and keeps the current value listed **when `allow` rejects it** (a caller that narrows `groups` itself before the picker sees them, e.g. an enum filter or a course-scoped star narrowing, is still responsible for its own value — see `entities.js` above `visibleGroups` for the full history). It lives here, not in picker.js, because this module imports nothing — picker.js imports preact through the browser importmap, which node cannot resolve). **Filtering lives at the CALL SITE** and is passed as `allow` — the segment builder passes its world-topology filter; the picker never learns a domain rule. Groups come from the server (`vocab.level_groups`/`course_groups`, `catalog.course_groups`, each segment's `origin`), never re-derived in JS. Call sites: segments.js clause params, header.js target modal (Course+Star collapsed to ONE control), routes.js step editor. Pinned by `tests/test_ui_picker_parity.py`: a shape scan flags any `.js` under `ui/` that renders `<option>` alongside a raw domain-vocabulary reference (`catalog.courses`, `vocab.stars`, `.origin.region`, etc.) unless it's in that test's explicit allowlist — add a call site by using `GroupedPicker`, not by growing the allowlist; a second guard strips comments from picker.js and asserts no domain word (course/star/level/segment/topology/route) appears in what's left |
| Library grouping (routes + segments) | `ui/group.js` (`buildTree` — the grouping ENGINE: `{of,label,order}` per level, `/`-joined node paths, numeric-aware and array-composite sort keys) + `ui/components/grouplist.js` (`GroupedList` + `useOpenGroups` — the collapsible chrome, ANY depth; indent is expressed by the DOM nesting itself — ONE `.lib-group` rule (margin + padding + guide line) that COMPOUNDS with depth, NOT a `--depth` custom property, which was dead CSS removed 2026-07-24. The depth-0 exception that cancelled the indent left sub-group HEADERS flush with their parent and was reverted 2026-07-25; the general rule — anything implying parent/child indents the child one level — lives in `.agents/skills/sm64-uiux/`). Consumers supply only their grouping POLICY and row renderer. Open state is an open-SET in localStorage per consumer key (`sm64.routeCatsOpen`, `sm64.segOriginsOpen`) so "nothing stored" = all collapsed; a new consumer MUST use a new key. Rows indent by margin with `width: auto` — `width: 100%` plus a margin overflowed the column and gave the library a horizontal scrollbar (live report 2026-07-24) |
| Route library grouping | `ui/components/routes.js` — feeds `buildTree`/`GroupedList` (above) a two-level POLICY: `category` is a free-text PATH (`"Main Categories/16 Star"`, `"Stage RTA/Wet-Dry World"`) split on `/`, so sub-categories needed no migration, no second column and no API change (a category containing `/` would be ambiguous). Seeded groups lead (Main Categories, Stage RTA), user groups follow, Uncategorized last; sorting is numeric-aware so star counts read 0/1/16/70/120. Category assignment is `CategoryModal` (a select per level listing existing names + "New…") — offering the existing names is what stops free text fragmenting. Clearing one needs `category: null` to survive the PATCH, hence `model_dump(exclude_unset=True)` in api.py. `.library-actions` is TWO columns: three icon+label buttons overflow a 240-300px column |
| Segment library grouping | `ui/components/segments.js` — nests **castle region → place**, both read off the server's `origin` stamp on `GET /api/segments`; ORDER comes from `vocab().origins` (the JS looks up a position and never re-derives region membership). A non-empty search passes `forceOpen` so hits are never dropped into collapsed groups. The editor's "Library category" select pins an override via `POST /api/segments/{id}/origin`, with "Auto" NAMING the detected place. Saving KEEPS the saved segment open (`onSaved(savedId)` → re-select from the reloaded rows): closing the editor dropped the user to the empty state with no way back to a just-created segment but hunting the library (live audit 2026-07-25). Segments only (rule 11): the star-side equivalent is the picker modal (spec 2026-07-24-segment-origin-categories §B), not a second library grouping — stars are already organised by course everywhere and have no "library" to group |
| Route builder UI | `ui/components/routes.js` — Routes tab: route picker + CRUD, step editor (reorder / add / remove / `need` for K-of-N groups), candidate chips, import/export panel. Display names + per-step/cumulative % from `GET /api/routes/{id}`; raw steps from `GET /api/routes`; every edit PUTs steps and re-fetches. Also hosts the "Run starts when:" trigger picker (reuses segments.js `ClauseRow`) |
| Route Practice focus | `ui/components/practice.js` (RouteFocus) — active-route picker (localStorage `sm64.activeRoute`) replaces the normal lists with the route's members in order; current step = live target (else step 1) and renders the full Star/SegmentSection inline; click a candidate to set target/retry. Route order + %s from `GET /api/routes/{id}` |
| Run view UI (Run tab) | `ui/components/runview.js` — route picker (selecting a route ARMS it — **no Start button**); **always-on clock** (idle=`0:00`+offset + route preview, active=ticks, finished=frozen only when the LATEST run finished) ticking client-side off `started_utc`+offset **minus paused_ms**; per-step cumulative + ± vs PB + gold ★; **Pause/Resume/Reset**, **Focus** (neutral), **click-to-hide** any timer (localStorage `sm64.runFocus`/`sm64.runHidden`); run-history list + progression graph (0-based, slower=higher, gold=PB) with click-to-expand splits, from `GET /api/run/history`. `store.js` holds `run`, refetched on `run_*`/`game_reset`; `build_run_view` carries per-step `pb_elapsed_ms`/`gold_ms` |
| Live event feed | `ui/components/feed.js` |
| Top control bar | `ui/components/header.js` — session/target/clock/rec/updates + the tab-independent "⏱ <segment> running" armed chip (arming RETIRES a star target; names from store.js `armedNames`, fed by segment_armed + view reconcile) + Rank picker + Star icons control |
| Per-star event graph | `ui/components/timeline.js` (marker styles via `MARKERS` registry) |
| Completion-time graph | `ui/components/progress.js` — per-star AND per-segment; gold = saved PBs; node click → practice.js `useGraphPick` reveals + scrolls to the row, auto-opens saved replays — the SAME path the clickable PB tag uses (`PbTag`, so `sec.pb[mode]` carries `attempt_id` from views.py) |
| Replay player + recording dot | `ui/components/replay.js` (incl. BufferSettings panel) |
| Compare tab UI | `ui/components/compare.js` (my-run vs comparison, one centered transport) + `ui/components/videosync.js` (`useSyncController` drives N `<video>` in lockstep; `VideoStage` forwards its element via `onEl`; `WorkArea` in/out handles + click/drag-to-scrub playhead) + `ui/frame.js` (shared game-frame step/jump, also used by replay.js) |
| Failure compilation UI | `ui/components/failcomp.js` — shared `FailureCompilation({identity})` (X/Y inputs in localStorage, Generate → poll → summary + Reveal); mounted in BOTH practice cards' detail drawer (`practice.js`), pinned by test_ui_section_parity.py |
| Rank UI (badge/banner/table/route medals) | `ui/components/ranks.js` (Medal/RankBanner + registry mirror + `RANK_MODE_OPTIONS`) + `standards.js` (collapsible editable table; columns = store ∪ section strategies; × is dual-meaning — on a SEEDED strat clears its standards, on a CUSTOM strat DELETES via `?purge=true` with confirm; each cutoff time links to its tier's example video, Edit-mode ▶ per-cell override, "Examples on xcams ↗" link; `markerPosition` draws a "you are here" mark BETWEEN two ladder rows — your time is essentially never AT a cutoff, so it is a 0..1 fraction of the gap the current grading basis falls in, interpolated against the ACTIVE strategy's OWN ladder rather than the entity's best-possible one so the bracketed cutoffs can never disagree with the rows on screen; the score shown next to it is never recomputed in JS, it rides `sectionRank.score` from `tracking/views.py`'s `_section_banner`/`ranks/scoring.py::score_for`); wired into practice.js (section banner + attempt medals), progress.js (medal nodes), routes.js + RouteFocus (per-step medals + route avg); banner "avg of N" basis line + mode-aware unranked sentinel |
| MARELO crest + header bar | `ui/components/marelo.js` — `Crest` (the division-numeral badge, mirrors `ranks/scoring.py`'s `DIVISION_NUMERALS` + `ranks.js`'s `RANK_COLORS`; a CREST not a medal on purpose — section medals are per-strat/per-entity, and an aggregate that looked identical would read as "just another star's rank") + `MareloBar` (fixed **44px** header pill, OBS-stable height; mounted in `header.js`'s `.marelo-row`, NOT the Rank tab, so it is visible from every tab). `division_progress` (how far into the current division the bar's fill sits) is computed server-side (`ranks/scopes.py::_division_progress` — it owns the band edges) and is never re-derived here |
| Rank tab (scope picker / card / chart / breakdown) | `ui/components/rankpage.js` — ONE view under three scope kinds (Overall / route / course) instead of three near-duplicate pages; the scope picker defaults to `GET /api/marelo/scopes`' `active` (the focus route — spec section 3.4) until the user deliberately browses elsewhere. `t.mareloRev` (bumped by store.js on every `REFRESH_ON` event, not just a scope change) is a fetch dependency, so the tab does not go stale while left open during play. A scope switch clears the OLD scope's card/chart/breakdown UP FRONT, so a `404` on the NEW scope can never leave stale data sitting under the new scope's label. `Breakdown` defaults to route order for a route scope, else biggest-gain-first; `HistoryChart`'s tier gridlines mirror `ranks/scoring.SCORE_ANCHORS` as a constant-table (not an algorithm — same convention as ranks.js/format.js) |
| Rank-up celebration overlay | `ui/components/celebrate.js` — climbs every tier gained one step at a time (`RANK_NAMES` slice, reversed) rather than teleporting, so a multi-tier jump reads as a real climb; acks (`POST /api/marelo/ack`) only once actually shown, but a celebration dismissed WITHOUT showing (the `sm64.celebrate` localStorage pref off) still acks immediately so the watermark doesn't re-fire next load. Mounted at the app ROOT in `app.js`, not inside the Rank tab, so a rank-up earned while on Practice still celebrates (rule 10 parity — no `desktop/`-only copy). The overlay itself (`.rankup`, fixed `inset:0`) is `pointer-events: none`; only `.rankup-card` is `pointer-events: auto`, so it never eats a click meant for the game |
| Shared modal shell | `ui/components/modal.js` — `Modal({title,onClose,footer,children})`; onClose optional (absent = not dismissable) |
| Active-strategy picker (star + segment) | `ui/components/stratpicker.js` — THE strat dropdown + "new strat…" modal for a practice card; owns the `POST /api/strat` write, the dropped-write alert, and the phantom-pick snap-back. ONE component for both kinds (`identity` = kind-dispatched body). Parity pinned by tests/test_ui_section_parity.py. Write target injectable (`submit` prop) — practice cards set the ACTIVE strat, attempt rows reclassify THAT attempt via `POST /api/attempts/{id}/strat` (`blankLabel`/`highlightUnset` tune the row variant). `allowBlank=false` drops the "no strategy" option entirely; both call sites pass `!sec.default_strat`, so a seeded castle movement (default `"Standard"`) cannot be unset — the server enforces the same rule (projection caveat 17), and `purge_strategy` 409s on a segment's default so the list can't be emptied from the ranks side either |
| Strategy-creation modal | `ui/components/stratmodal.js` — name + full rank ladder (time + optional example video per rank) on the Modal shell; Save rides the existing ranks endpoints (create→PUT thresholds→PUT videos, idempotent re-Save); opened from the practice strat dropdown, the standards table's + Strategy, and the header target picker |
| Update popup | `ui/components/update.js` — modal: version + patch notes (escaped-then-rendered) + GitHub link + exact `download_bytes` + Update/Skip/Later; polls `/api/update/status`. Backdrop/Esc dismiss as "Later" (offer) or "Close" (failed); inert during an active install. Mounted at app root in `app.js` (browser↔GUI parity) |
| Per-star external links | `links.py` — Ukikipedia RTA-guide URLs + `xcams_url(entity_key)` (the xcams Daily Star page, identity-derived from the live-confirmed `…/home/history?star=<abbrev>_<id>` pattern; secret-star prefix VERIFY) |

## UI verification norms

- After any UI change, verify with a headless render or the chrome-devtools
  MCP — unit tests + `node --check` alone shipped an invisible feature once.
  Harness-page technique + headless-Chrome fallback: see auto-memory
  `verify-ui-effects-with-harness-page`.
- Kill any `python -m http.server` you start, in the same session.
- **A harness must mount inside the real ancestors, or it measures a layout
  that does not exist** (rank-banner ellipsis, 2026-07-25 — measured wrong
  three times, twice by eye and once by a harness). Two clauses, both
  load-bearing:
  1. Inside the real **app shell**. `.app-shell` is
     `grid-template-columns: var(--sidebar-wide) minmax(0,1fr)` — 206px,
     dropping to `--sidebar-rail` 76px at 1180px and `display:none` under
     760px. A harness rendering into bare `<body>` hands every card ~206px
     it never gets, and reported ZERO overflow at 1400px when the real
     answer was eight.
  2. Inside the real **`.practice-page`**, which is what declares
     `container-type: inline-size`. Without it every `@container` rule
     silently never matches, so the harness cannot show a container-based
     fix working and reports it as ineffective when it is correct — a worse
     failure than the width error, because it looks like a verdict.
  - Sanity check before trusting any number from a harness: compare its
    rendered card width against a real screenshot of the app.
- **Sweep the width continuum, not three sample points.** For the rank
  banners, 1400/900/700 all passed while every window from ~1101px to
  ~1500px was broken — 900px passes only because the layout stacks there.
- **Viewport width is often the wrong signal in this app**: the sidebar step
  at 1180px means the pane a card lives in is NOT monotonic in window width
  (a 1181px window gives a card a 947px pane; a 1180px window gives it
  1076px). Gate card-internal layout on `@container` against
  `.practice-page`, the way the star row's `cqw` sizing already does.
- When a `@container`/media rule `display:none`s an element, its own `title`
  is not a fallback — a hidden element cannot be hovered. Move the text onto
  an element that is always rendered (the rank banner folds its basis line
  into the progress track's tooltip).
