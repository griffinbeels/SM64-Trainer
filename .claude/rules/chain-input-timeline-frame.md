---
paths:
  - "src/sm64_events/inputs/sampler.py"
  - "src/sm64_events/inputs/store.py"
  - "src/sm64_events/inputs/track.py"
  - "src/sm64_events/inputs/service.py"
  - "src/sm64_events/replay/recorder.py"
  - "plugin/gfxwrap/gfxwrap.c"
  - "src/sm64_events/replay/pluginsource.py"
  - "src/sm64_events/replay/framestream.py"
  - "src/sm64_events/replay/oracleread.py"
  - "src/sm64_events/replay/ledger.py"
  - "src/sm64_events/replay/ffmpeg_sink.py"
  - "src/sm64_events/replay/extract.py"
  - "src/sm64_events/replay/frameclock.py"
  - "src/sm64_events/replay/mapalign.py"
  - "src/sm64_events/replay/feedmap.py"
  - "src/sm64_events/replay/padread.py"
  - "src/sm64_events/replay/timerread.py"
  - "src/sm64_events/replay/service.py"
  - "src/sm64_events/ui/frame.js"
  - "src/sm64_events/ui/components/inputtimeline.js"
  - "src/sm64_events/ui/components/attemptdrawer.js"
  - "src/sm64_events/ui/components/controllerpanel.js"
---
# Chain: which game frame the paused video picture shows, and the pad the panel draws for it

Read this before editing any module named below. A symptom is seen at the
SINK; its cause sits at the FIRST hop where the value stops being true, and a
fix downstream of that hop cannot hold. Before any fix: run the counterfactual
(force the known-good value in at a hop, re-run to the sink, see whether the
symptom vanishes) — the earliest hop whose correction prevents the failure is
the one to fix. Walk the table only when the counterfactual is ambiguous.

- **Value:** the game frame a video picture shows, and therefore the pad the input timeline's panel draws beside that picture.
- **Source truth:** for a clip recorded through THE CAPTURE LAYER (item 95,
  `plugin/gfxwrap`, `replay/pluginsource.py`): the frame counter the plugin
  copied out of RDRAM inside Project64 at the ProcessDList that drew the
  picture -- the ledger row says `exact`, and nothing downstream infers.
  THE ORACLE for any map (`replay/oracleread.py`, `tools/score_oracle.py
  --attempt N`): the frame number Usamune's HUD memory display of
  `0x8032D5D4` prints into the picture, read map-free through the +1 rule.
  For a desktop-grab clip: Usamune's displayed CLOCK joined to the
  coherent `(gGlobalTimer, usamune_overall)` pair captured for that picture;
  for pad identity, the pad per game frame in RAM, read by the sampler at 250
  Hz (`InputSampler.health()["edge_mismatches"]` is its self-check) and audited
  by Usamune's input display painted into the picture. The CLOCK glyphs alone
  are not proof: exact relative-time inversion neither proves five-glyph OCR
  nor supplies an absolute game-frame epoch.
- **Sink:** the panel under the timeline (FRAME n / N, the stick box, the button chips) on the picture the `<video>` is presenting.
- **One clock:** the picture feed (item 38, 2026-09-02) puts hops 4-7 on the pictures' own composition times -- seconds since the run's spawn, carried in the clip as `frame_times` -- so a frame's time in the browser IS the time the ledger stamped it. Hop 5 now checks that bookkeeping against a second captured relation: the screen CLOCK value and its same-picture RAM counter. The CFR feed (clips from before it, the in-process fallback, `picture_feed=False`) has no one clock end to end: hops 1-3 run on the GAME frame counter, hops 4-6 on wall-clock capture time and a 60 Hz encode grid, hop 7 on the browser's `mediaTime`, joined only by the inferred map.

| # | hop | value is true here as | module | probe (reads it) | inject (forces it) | when the hop is broken, the probe shows | when the probe itself is broken, it shows |
|---|-----|-----------------------|--------|------------------|--------------------|------------------------------------------|--------------------------------------------|
| 1 | the pad in RAM, per game frame | `InputFrame(stick_x, stick_y, buttons)` filed under the gGlobalTimer value read in the same poll | `src/sm64_events/inputs/sampler.py` | `uv run python tools/probe_inputs.py` (live) / `InputSampler.health()["edge_mismatches"]` | none -- domain rule 6 forbids writing emulator memory | `edge_mismatches` > 0, or a `skips` count naming frames nobody read | a green health payload from a server attached to nothing |
| 2 | the chunk store, then the track | the same frames, trimmed to the attempt: the run ends the frame BEFORE the first dance action, `first = last - (igt - 1)` | `src/sm64_events/inputs/store.py`, `src/sm64_events/inputs/track.py` | `uv run python tools/inspect_timeline.py --attempt N --frames a-b` (prints the pad per panel frame) | append edited frames with `db.inputs.append` (the shape `tests/test_inputs_track.py` uses) | frame 0 landing before the reset was pressed (5534: the fall counted as the dance; four frames early) | a track read from a sibling worktree's journal |
| 3 | the timeline payload | `runs` on the capture axis (zero-based at the clip span's first captured frame), `lead_frames`, `attempt_frames`, `stretches` -- the service's `timeline` call | `src/sm64_events/inputs/service.py` | `InputsService(...).timeline(id, span=(lo, hi))` offline, then `run_at(axis)` | none -- pure over hop 2, so hop 2's injection reaches it unchanged | a run whose `stick_x` differs from hop 2's at the same raw frame | a payload built without the clip's span (the attempt alone; a different axis) |
| 4 | the pictures the recorder grabbed, and their encode | THE CAPTURE LAYER (item 95): the wrapper plugin inside Project64 copies the tracker's address table out of RDRAM at the ProcessDList that draws frame N and, at the VI whose VI_ORIGIN changed, reads GL_FRONT itself when the emulation thread holds a GL context, else asks the wrapped plugin's own ReadScreen (GLideN64_LINK_4.2 renders on a thread of its own, so it is always the latter for him -- measured 2026-09-05; the header's STATUS_READSCREEN bit says which), publishing (picture, stamp) into the frame stream; the recorder commits to this source only after `pictures_flow` sees a first picture; otherwise it records through `DesktopUntilLayerPresents` -- the desktop grab watching the heartbeat, which ends itself like a lost window the moment a picture flows so the next attach gets the plugin, in whichever order the game and the trainer were opened -- and `/api/replay/status` `frame_source_note` says why while it waits; `PluginVideoSource` decodes the stamp with the sampler's own decoder and the recorder files the row with `exact: true`, `frame` = the stamp's counter, `pad`, `mario`, `igt_overall` -- the frame clock is not consulted, and at extraction `_map_from_feeds` joins those rows to the clip's pictures with NO display lag and `None` on an inexact row (the map IS the stamps; `picture_igt` beside it is the game's own timer per slot, which the inspector shows as the frame's time). Otherwise THE PICTURE FEED: each distinct desktop grab stamped with composition time + the RAM frame + the coherent `usamune_overall` value sampled inside that frame's counter sandwich (the picture ledger). Either way the sink writes each picture ONCE into a NUT stream at its composition time, files the write in the feed log, and ffmpeg encodes it passthrough -- one video frame per picture at its own time (VFR), audio in the same stream on the same clock. The CFR feed re-times the latest grab onto a 60 Hz grid on the wall clock instead | `plugin/gfxwrap/gfxwrap.c`, `src/sm64_events/replay/framestream.py`, `src/sm64_events/replay/pluginsource.py`, `src/sm64_events/inputs/sampler.py`, `src/sm64_events/replay/recorder.py`, `src/sm64_events/replay/ledger.py`, `src/sm64_events/replay/ffmpeg_sink.py`, `src/sm64_events/replay/extract.py` | `/api/replay/status` `frame_source` (plugin / desktop) and `frame_source_health`; the sidecar's `picture_ledger` row carries `exact`, `frame`, `pad`, `igt_overall`; `tools/score_oracle.py --attempt N` scores the rows' frames against the number printed in the picture; `tests/test_gfxwrap_host.py` drives the plugin without an emulator | the host's `--drive` writes a known counter into fake RDRAM and a known colour into the picture: the stamp and the pixels must come back equal | the pair is absent on an older/unsupported layout, or `frame` and IGT came from opposite sides of a game-frame edge (the sampler's sandwich test catches this); the ring holding fewer frames than the feed log; CFR: 1 / 2 / 3 slots per picture jittering | a probe reading the ring segment instead of the extracted clip (segments start at pts 1.4) |
| 5 | the frame map: video slot -> game frame | for a stamped picture with an advancing readable CLOCK: `shown = ceil(centiseconds*3/10)`, `lag = usamune_overall - shown`, `frame = gGlobalTimer - lag` (`timerread.py`). The join is local per picture and assumes no run-wide epoch (`counter_epoch.py` proves the historically misnamed RAM counter restarts at subarea loads). Illegal glyph values, impossible lag, or transition spikes are rejected; an isolated rejection plus frozen/unreadable/unpaired slots are explicitly counted bookkeeping bridges, while a sustained rejection refuses the whole timer path. The pad reader then audits without mutating timer identity. The lag is a PER-PICTURE measurement, never a constant: 1 on every picture of two clips and 0 on a third from the same sitting (2026-09-04), and it may DROP between consecutive pictures (the display catches up), so the transition guard forbids only a backward displayed frame. Older/no-stamp clips retain the feed-log/pad compatibility path; `tools/remap_clip.py` re-runs this hop on a clip already cut | `src/sm64_events/replay/feedmap.py`, `src/sm64_events/replay/frameclock.py`, `src/sm64_events/replay/timerread.py`, `src/sm64_events/replay/padread.py`, `src/sm64_events/replay/service.py` | sidecar `timer_reading` (`mechanical`, `bridged`, `rejected`, observed lag range) + independent `pad_reading`, beside `feed_match` | hand the timer a deliberately wrong prior map while giving it a coherent RAM pair; its mechanical slots still return the formula's map. Then make RAM reset while screen does not for >3 slots; it must refuse | timer identity follows the corrupted prior despite readable coherent slots, or a sustained counter-domain mismatch ships as a bridged timer map | 100% claimed from legal final digits/monotonicity alone; a bridge reported as mechanical; pad audit scored only after its own alignment |
| 6 | the clip's timestamps as the browser sees them | picture feed: frame k presents at `mediaTime = frame_times[k]`, every frame's own pts read off the cut (ffprobe) and carried in the view; CFR: `k/60 + video_start_s`, the cut's `-avoid_negative_ts make_zero` leaving the first pts at the cut's sub-frame remainder | `src/sm64_events/replay/extract.py`, `src/sm64_events/replay/service.py` | `uv run python tools/probe_clip_seek.py --attempt N --slots a-b` (serves the clip to headless Chromium, seeks with the shipped `src/sm64_events/ui/frame.js` clip clock -- into each frame's own span, or the grid from the first pts -- and reads `mediaTime` back) | that probe's `fixed` column IS the injection: the seek through the clip clock | seeking to the grid's time for slot k presents k-1 (5782, CFR: measured every seek one picture early); a VFR clip stepped on a 60 Hz grid lands on the wrong picture wherever a gap is not 1/60 | a server without Range support: every seek snaps to 0 and reads as "the clip cannot seek" |
| 7 | the panel's lookup | `slot = slotAtTime(mediaTime, clock)` -- `frame.js::clipClock` builds the clock from the view: the last frame begun at or before the time for a picture-feed clip (`frame_times`), `floor((t - video_start_s) * fps)` for CFR -- then `raw = frame_map[slot]`, axis via `stretches`, pad = the run containing it | `src/sm64_events/ui/frame.js`, `src/sm64_events/ui/components/inputtimeline.js`, `src/sm64_events/ui/components/controllerpanel.js` | drive the real page (uilab) and read `.input-inspector-frame` + `.stick-value` on a paused picture; compare with hop 6's `mediaTime` | set `frameMap`/`clock` props by hand in the fixture | the readout one frame behind the picture on screen (his 5782 screenshot: picture R2, panel 157 / "--") | the fixture's synthetic view (its map is 2 slots per frame from 30 fps, not a real clip) |

## Counterfactual recipe

For a report of the shape "the screen shows X, the panel shows Y on the same
paused picture":

1. Hop 3: build the payload offline with the clip's span and read the run at the
   panel's axis. If it holds Y where hop 2 holds X, the fault is the run
   collapse or the axis; stop there.
2. Hop 5: `tools/score_pad_read.py` on the clip -- but read its DISAGREEMENTS
   and the reader's own cells at the slots around the picture, not its
   percentage. Decode those slots with `-frames:v` capped (an uncapped `trim`
   printed 501 tiles and an off-by-one that was the probe's, not the clip's).
   On a newly stamped clip, inspect `timer_reading` first: force the expected
   `(gGlobalTimer, IGT)` pair and verify the direct formula reaches the panel.
   A long run of rejected timer slots is a counter-domain/OCR failure at this
   hop, not permission to shift the prior map through it.
3. Hop 6: `tools/probe_clip_seek.py --attempt N --slots a-b` around the
   picture. If Chrome presents slot k-1 for the old seek meant for k and the
   corrected seek presents k, the fault is the clip's first pts and the
   panel's slot arithmetic, and no reader fix can hold. `ffprobe -show_frames
   -read_intervals` prints the pts themselves when the probe's numbers need
   a second witness.

## Failure catalogue

- 2026-09-05 hop 4/5, THE FIRST THREE PLUGIN CLIPS (6918, 6942, 6986: `frame_map_source` plugin, `pad_stamp_agreement` 974/974, 355/355, 560/560, 2-3 inexact rows each) and what they showed. (1) "the time is always one frame later" / the pad reader's 41 disagreements at stick transitions: `inspect_timeline --audit` put the map exactly +1 behind the stamps on all 1074 pictures -- `_map_from_feeds` was still subtracting the desktop grab's `DISPLAY_LAG_FRAMES` from rows that already name the frame that DREW the picture, and `_take_the_stamps` only relabelled the source. Now a clip whose rows all carry `exact` joins with lag 0 and `None` on an inexact row. (2) The rows carried no `igt_overall` at all: the plugin's RDRAM span was measured once at InitiateGFX, before PJ64 committed the expansion pak, so `usamune_overall` (0x417C72, above 4 MB) was refused all session -- the span is measured again at RomOpen and whenever an entry sits past it but inside the tracker's claim; the host's `--commit-late` proves it. (3) The inspector's time was `frame - lead` over 30 -- the track's own count, which the track cuts to the attempt's FINAL time, so it starts a frame or two apart from the on-screen timer by construction; on a stamped clip the inspector now shows `picture_igt[slot]`, the IGT the game held when it drew the picture on screen (`inspectorClock`, `is-stamped`), and counts only where no stamp exists. (4) "frame -1 to frame 3 on one step": real -- the level load presented no picture for 1204-1205 and one inexact picture for 1206 (two lists before one present); with the lag gone the step reads 0 -> (none) -> 4, which is what the game showed.
- 2026-09-05 hop 4, THE SECOND LIVE SESSION -- "Frame-exact capture is off. Set up" still, after the restart, and "order shouldn't matter". MEASURED: the new DLL WAS loaded (its log notes every callback, all without a context, and the header's heartbeat moved 60/s with `want_frames` 0), but the recorder attached to Project64's window at 02:51:41Z, two seconds before RomOpen, when the heartbeat was still, chose the desktop grab, and never revisited the choice -- `frame_source` desktop, `frame_source_note` null. So the camera choice is now revisited: `DesktopUntilLayerPresents` wraps the desktop grab with a watch on the heartbeat and, once `pictures_flow` sees a picture, calls `on_stopped` so the attach loop re-runs the factory and gets the plugin (a refusing layer is asked again every 15 s, the reason on the note meanwhile). Two more things the same restart showed: (a) the ERROR at boot -- `got 2360960 bytes; need 2364928 bytes` -- was a 1190-px-wide start-up window: FFmpeg pads a plane's rows to 32 bytes (4760 -> 4768), PyAV's `update` refuses the raw bytes, and the sink respawned ffmpeg on every picture until the window grew to a multiple-of-8 width; `ffmpeg_sink.fill_plane` lays the rows out at the plane's line size now, so no window width records nothing; (b) the setup screen's first read after boot compared the heartbeat with nothing and said "restart Project64" for a running game -- it takes a second reading 50 ms later. Clip 6791, cut off the desktop grab in that window: `inspect_timeline --audit` puts the map 25 frames off the stamps (mode +25, 394 pictures encoded of ~539 expected, "capture was degraded") -- his "about 25 frames too early" exactly; that is the inference path under a degraded capture, the thing the layer replaces, and it is not being re-tuned.
- 2026-09-05 hop 4, THE FIRST LIVE SESSION -- "view replay is broken": `Replay unavailable: no footage in the replay buffer`, with the setup screen reading a green Active and the timeline saying frame-exact capture was off. Three facts, all MEASURED: (1) the layer was loaded and its heartbeat moved, but `dropped_by_plugin` climbed 33/s with `delivered` 0 -- `wglGetCurrentContext()` returned NULL on every UpdateScreen. The review's guess (threaded video, a setting to turn off) was WRONG: GLideN64_LINK_4.2 has no `threadedVideo` key at all; its RTTI names `ReadScreenCommand` and friends, and the test host driving that real DLL (`--cpu-thread --no-context --wrapped <path>`) shows no context on the calling thread at InitiateGFX, RomOpen, ProcessDList or UpdateScreen, and a host whose window thread is also the caller deadlocks in RomOpen -- that build runs every GL call on a render thread of its own, unconditionally. (2) Its ReadScreen is a command that thread executes in order after the swap: through the host it returned a 640x480 buffer on every present, 0 dropped, and HeapValidate accepted every buffer (static CRT, linker 14.30, so malloc is the process heap) -- the plugin now takes that path whenever its own read has no context, frees through HeapFree after validation, and sets STATUS_READSCREEN; a buffer that fails validation retires the path rather than being freed blind. Every refusal now leaves a line in `sm64_trainer_gfx.log` with the thread and context per callback. (3) The recorder had committed to the plugin source on the heartbeat alone and sat on an empty ring for an hour: `main.py`'s factory now asks `pluginsource.pictures_flow` for a FIRST picture (0.6 s) before choosing it, else records through the desktop grab with the reason on `/api/replay/status` `frame_source_note` and the setup screen's row -- which no longer shows a bare Active while `problems` holds one, and offers Update when this build's DLL is newer than the installed one (`CaptureLayer.refresh_if_stale` copies it at boot while PJ64 is closed). What is still owed to his live gate: whether the ReadScreen picture IS the presented frame (the oracle, item 3), and its cost per present at his window size (item 6).
- 2026-09-04 hop 4, THE CAPTURE LAYER'S FRESH-CONTEXT REVIEW (before it ever ran in his emulator) -- four things the test host could not see, each fixed in `plugin/gfxwrap/gfxwrap.c` and two of them now driven by the host: (1) a stamp entry above the committed RDRAM (the tracker claims 8 MB; `usamune_overall` sits above 4 MB) would have read past the allocation and taken the emulator down -- the copy now runs under a structured-exception guard and a `VirtualQuery` span, and the host reserves 8 MB / commits 4 to prove an overrun becomes an empty entry; (2) an ini naming the wrapper itself recursed until the stack died -- the wrapper now recognises its own module and stays unwrapped; (3) the GL read assumed the window's framebuffer and no pack buffer were bound, which GLideN64 (FBO- and PBO-heavy) does not promise -- the read now binds framebuffer 0 and pack buffer 0, zeroes the pack row/skip parameters, and restores every one of them; (4) PJ64's status bar sits INSIDE the client rect and the wrapped plugin draws above it, so the read starts that many rows up (`hStatusBar`'s height) -- unverified until his live gate, since the host has no status bar. And on the reader's side: (5) a row is `exact` only when exactly ONE display list ran between presents -- zero or two leaves which list the pixels came from an inference, and extraction keeps the plugin map only while 98% of rows are exact, saying `frame_map_inferred` otherwise; (6) a plugin that died with the header's bits still set no longer holds the recorder on a dead source (the plugin clears its bits at detach; the reader also checks the plugin's process); (7) a recreated mapping (seq back to 1) no longer strands a reader holding a high seq; (8) the event and process handles go through ctypes as pointers, not C ints; (9) six ring slots (~200 ms) where three were ~100 ms of buffer; (10) the Plugins dialog is answered from the ini alone (PJ64 enumerates every DLL in its Plugin folder by loading it, asking GetDllInfo and freeing it, so a wrapped plugin loaded there leaked one reference per pass) and the real plugin loads at InitiateGFX, released at CloseDLL; (11) paths go through the wide APIs and every refusal (a mapping that would not open, a wrapped plugin that would not load, an unwrapped InitiateGFX) leaves a line in `sm64_trainer_gfx.log` beside the DLL, the one place the setup screen cannot see into; (12) detach under a FreeLibrary clears the header, detach at process exit touches nothing; (13) a decode error on one slot is counted, never fatal to the source. The host now also drives a dead emulator (the source must stop within seconds) and a wrapped plugin that leaves an FBO, a pack buffer and odd pack parameters bound (the colours must still be the window's).
- 2026-09-01 hop 2 — "frame 0 is before the reset... frame 3 is the first frame I actually reset on" — the run ended at the first grab ACTION (a midair fall), so `first` landed four frames before the reset press — fixed in `track.py` (the dance's first frame ends the run; the fall is timed).
- 2026-09-01 hop 7 — "84 2 on screen, 84 in our tool, yet screen-checked 100%" (5782) — the map is RIGHT (slot 496 = frame 157 = R2, sharp minimum at shift 0); Chrome presents slot k-1 for the timeline's seek to k because the clip's first pts is 0.011 s (2/3 of a slot) and the seek targets `(k+0.5)/60`; the panel then reads the presented frame honestly and shows the earlier pad — fix belongs at hop 6 (zero the video's first pts at extraction, and carry `start_time` to the panel for older clips), never in the reader.
- 2026-09-01 hop 5 — the METER: `score` counts the objective `align` minimised, so it returns the same number for a correct map and one corrupted by +2 inside an evidence-free hold (fresh-context review, T1). An honest meter reports agreement, COVERAGE (the display can distinguish only ~17% of frames from a neighbour — holds dominate) and EXPOSURE (the longest unpinned run), computed against the map the reader did NOT align.
- 2026-09-01 hop 4 — the structural finding: one video frame per distinct captured picture, stamped with the ledger's RAM frame at encode time, would make the map an identity plus one offset (item 38's approved recorder change, never built). Everything downstream is recovering an identity capture threw away.
- 2026-09-01 hop 6, the FIX itself -- `tools/probe_clip_seek.py` shipped and its first run on 5782 printed `first video pts 0.000000` with the corrected seek still one early: `video_start_of` built the ffprobe path by replacing `ffmpeg` in the whole path, which renamed the install folder (`D:/ffmpeg/bin/ffmpeg.EXE` -> `D:/ffprobe/bin/ffprobe.EXE`) and fell to the 0.0 fallback -- the shipped fix would have changed nothing on his machine. Fixed by file-name replacement + PATH fallback (`ffprobe_beside`). The lesson is the one the probe column exists for: a fix at the right hop still needs its probe run against it, because a fallback that returns the old assumption is indistinguishable from the fix working on a clip that never needed it.
- 2026-09-02 hop 4, BUILT -- item 38, the recorder change he approved: the ring holds one video frame per distinct captured picture at its own time. Three measurements shaped it, each a way the obvious version was wrong: ffmpeg's `-use_wallclock_as_timestamps` stamps at its READ, and its scheduler reads a pipe in bursts, so a 30 pictures/s feed came out with 108 frames of one segment ONE 90 kHz TICK apart; a wall-clock-stamped audio pipe beside a stamped video stream throttled the video to 26 of 30 pictures/s whatever offset the two carried (lag +-2 s, no filter, null output: all 26; no audio: 30); and `-fflags +nobuffer` discards the packets stream analysis reads (23 of the first 83 pictures). The shape that measured clean: one NUT stream on stdin carrying video AND audio with this process's own stamps, `-fps_mode passthrough -enc_time_base demux`, per-segment timestamp reset off. Proof: sink -> ring -> cut -> feed map, 151 of 151 pictures, 0 unmatched, residual 0.15 ms median / 2.1 ms max; a flash and a click at one instant land together in the cut.
- 2026-09-02 hop 5, the pad reader's DECODE AXIS -- "basically every single frame is wrong" on a picture-feed clip (5814): the panel lagged the screen by a uniform ~2 game frames, and the reader's own screen-reads and its disagreement flags were wrong too. First divergence at hop 5 (fresh-context review, counterfactual: forcing the correct decode moved screen-vs-map agreement from +2 to 0 and 61% -> 96%). `padread.decode_cells`/`decode_icons` ran ffmpeg WITHOUT `-fps_mode passthrough`, so a VFR picture-feed clip (r_frame_rate 120) was re-timed onto its rate grid and 8 frames duplicated: 726 cells of a 718-frame clip. `read_clip` kept the first 718, every cell past the first dup shifted, and that sheared path was written back as the map. Fixed by decoding passthrough (one flag), so the reader's cell axis IS the clip's frame axis IS the browser's slot axis. The picture feed was NOT the fault and was NOT reverted. Also fixed: the ffprobe-path whole-string replace recurred here (`_clip_dims` now uses `ffprobe_beside`), and `tools/inspect_timeline.py` was CFR-blind (it read a VFR clip on a /60 grid and faked a ~55-frame stamp gap; now seeks via frame_times).
- 2026-09-02 hop 5, DUPLICATE PICTURES -- "seems like we're still struggling with duplicate frames in the video... Ideally when the user plays back their video, they NEVER see a duplicate frame." A picture-feed clip holds one encoded frame per CAPTURED picture, but when the game lags the emulator re-presents the same render: two grabs, two ledger rows with ADVANCING RAM stamps, one picture on screen. Measured on his pyramid clip (5946): 115 of 775 stored frames pixel-identical to their predecessor, and the map stepped +1 across 110 of them -- the panel's pad changed while the screen held, and the stepper landed on the same image twice. Root cause of the flag that should have stopped it: `padread.picture_flags` unpacked `picture_runs`' `(first slot, LENGTH)` as `(start, end)`, so `same[start + 1 : end + 1]` was an EMPTY slice for any run past the clip's opening -- the aligner's "may only dwell" constraint reached almost nothing. Fixed three ways, each measured: the run-length unpacking; `mapalign.decode_grey` decodes `-fps_mode passthrough` like the reader's (a VFR clip's runs must index the clip's own frames); and the picture runs are applied AGAIN after the reader answers, so one answer per picture is an invariant of the SHIPPED map rather than a hope about the reader. After: 115/115 duplicates step 0, 115/115 show their predecessor's pad, and the stepper can land on 0 of the 115.
- 2026-09-02 hop 4, TWO CUTS ONE PATH -- his 100-coin replay played as a black video: the H.264 stream was full of invalid NAL units and the sidecar counted 1921 frames of a file holding 1380. He clicked extract and the LBLJ autodetect re-opened the drawer, so two `view()` calls raced: `view` checked `clip.exists()`, both missed, and two ffmpeg processes wrote one output path. Fixed twice over -- `ReplayService.view` takes a per-attempt lock (the second caller waits and takes the cached clip), and `ClipExtractor.extract` cuts to `<name>.cut<pid>.mp4`, probes THAT file, and `os.replace`s it into place, so a partial or racing write can never be served and the sidecar can never describe a different file than the one on disk. The temp name keeps the .mp4 suffix LAST: ffmpeg picks its muxer from the extension.
- 2026-09-02 hop 4/5, A LOADED MACHINE -- "I would expect that this system is deterministic / unimpacted by external lag from other sources." Capture is real-time, so a loaded machine genuinely produces FEWER pictures: his Haunted Books clip captured 680 pictures, encoded 387 (20.4/s against ~30; the rest dropped at the sink's PICTURE_QUEUE) and the feed log then matched only 178 of 387, so `feed_map` refused and the clip shipped with NO frame map -- the panel fell back to plain arithmetic, which is "lots of incorrect frames". Fewer frames is honest; no map is not. The picture-ledger path now answers when the feed log cannot: it matches rows to the clip's own picture RUNS, which does not care how many frames went missing, and it is handed the clip's own `frame_times` (a VFR clip's runs never sit on the 60 Hz grid -- the third instance of that blindness). What is still owed: the drops themselves (a bigger queue, or a lower grab rate under load) and an honest surface saying the capture was degraded.
- 2026-09-02 hop 4, CAPTURED AND ENCODED IN LOCKSTEP -- his rule: "We should always be encoding frames we captured. I don't understand why lag means we don't encode the frames? If I see a frame in my replay, as a user, I would expect to see the input capture for that frame as well." He is right, and the old shape was indefensible: the sink's queue was 16 pictures deep and shed its OLDEST entry on overflow -- a picture the LEDGER HAD ALREADY RECORDED -- so under load the ledger described frames the video did not contain and the feed log could no longer cover the clip (his Haunted Books run: 680 captured, 387 encoded). Now the recorder asks `sink.has_room()` BEFORE `ledger.observe`, so a picture it cannot encode is never recorded as captured; the queue is bounded by BYTES (512 MB, ~65 of his frames, >2 s of slack) and drops nothing once queued. A loaded machine yields a sparser clip, never a clip whose map is a lie. `grabs_skipped` and `encode_backlog` ride `/api/replay/status`, so "this capture was degraded" is a number rather than a thinner-looking video.
- 2026-09-02 hop 5, THE READER IS AN AUDITOR, NOT AN ALIGNER (on a covered clip) -- his "if even ONE frame is wrong, that's unacceptable", against single-frame BUTTON errors in neutral stretches (Elevator Tour frame 13 drew R where the screen showed Cdown). A fresh review measured what the per-slot DP does: 50-77% of a clip's frames repeat their predecessor's pad, so the display cannot separate them, and 78-94% of the slots the DP MOVES off the feed log sit between two such frames -- unfalsifiable moves, and the source of the run-to-run flakiness. On the two clips whose feed log covered them (92%, 100%), the bookkeeping plus ONE flat integer equalled a HINDSIGHT per-window corrector and came within 0.3 and 0.5 points of the DP; on a 73%-covered clip the DP genuinely earned 10 points. So `feed_match.matched / frames` decides: >= 0.85 (and a sweep margin >= 0.15) ships `feed_log+offset` and the reader only reports; below, the DP aligns as before and the clip is marked `frame_map_degraded` so a repaired capture never looks like a clean one. The offset comes from a direct sweep over [-6, +6] of screen agreement (`padread.offset_sweep`) against the map the reader was HANDED -- no DP, not circular, and its margin is its own refusal criterion. RULED OUT in the same review: learning more button-icon templates. Press edges resolve only 3-10% of the ambiguity, because a held stretch is usually idle on the buttons too.
- 2026-09-02 hop 5, THE READER'S OWN COMMONEST MISREAD, invisible for the whole feature -- his question was the right one: "have we even FOR SURE confirmed that we can even accurately extract the numbers... out from the bottom left corner of the screen?" No, and it could not: every accuracy number was scored against a map the reader itself aligned. THE MAP-FREE TEST is the display's own GRAMMAR -- Usamune prints an axis as a bare `0` (blank letter, `0`, blank) or a letter beside a magnitude of 1..99, so a letter beside magnitude 0 is impossible with no map and no threshold involved. Measured on his Shoot into the Wild Blue clip (6510): **563 of 6316 axis-readings, 8.9%, were exactly that**. Cause: `ink_mask`'s `bright` clause counts any bright pixel as font ink, so a blank cell over a WHITE SKY is all ink and the silver U -- mostly white highlight -- matches it at distance 38 with a wide margin. Two consequences, both of which he reported: the disagreement list filled with false accusations against the timeline ("a lot of the screen checked frames aren't actually even wrong" -- 578 of that clip's 604 were the reader's own misread), and the aligner paid a unit for the letter at EVERY candidate frame in exactly the resting stretches where it has the most freedom, which is where his single wrong BUTTON frames appear. `padread.enforce_grammar` corrects it: impossible on his clip 8.9% -> 0.00%, agreement 80.8% -> 99.17%, misreads 595 -> 17, disagreements 604 -> 26. STILL WEAK, not the map's problem: with better letter reads the icon channel learns more buttons and agrees on only 760 of 1557 lit icons (it was A-only at 99%) -- a false-positive icon reader, same bright-background family; measured to change the alignment on that clip by NOTHING (icons on and off give identical maps), so it is a reporting weakness, not a correctness one. And `ink_mask`'s bright clause is the underlying cause of both and is still there; the grammar is a guard above it.
- 2026-09-04 hop 5, THE FIRST THREE CLIPS ON THE TIMER WIRING -- "Inside the Ancient Pyramid... just totally wrong. Massive mistakes" (6592), and "extraction took a LONG time... probably 10x" on all three. Three defects, one measurement. (1) The pyramid's display lag is 0 where BoB's and WF's is 1 (the pad display, scored with no alignment: stamp-0 97.8% vs stamp-1 69.8% on the pyramid; stamp-1 99.6% vs stamp-0 74.1% on BoB), and on 19 pictures it jittered to 1 -- REAL, the display agreed with the timer's own lag on 18 of them. `_transition_holds` forbade the displayed frame advancing more than RAM did, which is exactly what a lag drop looks like, and the two-deep spike stack then rejected 49 of the 50 pictures after two consecutive lag-1 ones: the timer REFUSED the clip. Fixed: the displayed frame may not go backward; how far it may jump forward is the lag range. (2) The pad reader's pre-alignment audit (16615f33) scored the raw map, whose unmatched slots are None, and crashed on `None + k` -- on every clip -- so the pyramid fell to the LEGACY ink aligner, which shifted the feed-log map -2 on weak evidence (fit 0.589, margin 0.108): his three screenshots were each exactly two frames early. Fixed: an unmapped slot is skipped. (3) `register()` searched 441 offsets x 5 boxes x 200 frames exhaustively: 77-82 s per clip. Now one shared coarse offset then a pixel of refinement per box: 3 s, identical reads (457/457/392 on 6611). The measurement that outlives the fixes: DISPLAY_LAG_FRAMES = 1 is wrong on one clip in three, so the feed-log map is bookkeeping and only the screen names the frame. After: the pyramid joins 605 mechanical / 114 bridged / 1 rejected and audits 664 of 679 (97.8%: 9 of the 15 are the display reader's own 3-vs-8 misreads, 2 sit in bridged slots, 4 are a magnitude off by one); the twins audit 99.8% and 99.9%.
- 2026-09-03 hop 5, TIMER READER DIAGNOSIS (round 32 item 91) -- the first divergence was the missing absolute join at hop 5. The prototype inverted a displayed CLOCK value exactly, then silently obtained the missing absolute epoch from controller OCR or the old map (4 of 8 runs on 6510 tied and took the old map). Exact inversion proves neither that all five glyphs were read correctly nor which absolute `gGlobalTimer` frame the relative value names. The repair records `(gGlobalTimer, usamune_overall)` coherently at hop 1/4 and joins each readable picture directly at hop 5; controller pixels are an independent audit only. Older address/layout comments said that RAM counter survived subarea loads, but the newer measured `counter_epoch.py` evidence proves it is leg-local; those comments are corrected here and the formula assumes no run-wide epoch. Isolated reset-edge mismatches bridge; a sustained mismatch refuses. Frozen/unreadable slots remain explicitly inferred (`frame_map_inferred` / `timer_reading.bridged`), distinct from capture degradation.
