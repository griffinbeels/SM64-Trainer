---
paths:
  - "src/sm64_events/tracking/caveats.py"
  - "src/sm64_events/tracking/pbaction.py"
  - "src/sm64_events/tracking/activestrat.py"
  - "tests/test_caveats.py"
  - "tests/test_pbaction.py"
---

# A saved time, a PB action, the active strategy — where to change what

Three questions the practice log and the PB commands both ask, one module
each. Lifted out of `.claude/rules/tracking-storage.md` on 2026-08-22 when
that file passed its size ceiling; nothing was summarised.

| To change... | Edit |
|---|---|
| Whether a saved time MEANS what the rank beside it implies | `tracking/caveats.py` — `caveat_for(pb_row, attempt) -> key | None`, THE answer, read by the practice card's PB tag, the quick-select star cells (`caveat_by_star`) and the segment cells (`segment_targets[].caveat`). Three findings converged on one sentence and share one module because two surfaces honestly computing the same fact and wording it differently is the divergent-duplication class. `unattributed` = the PB has no `strat_tag`, so `current_pbs_by_strat` can never find it whichever strategy is active — the live report "Bowser 1 shows PB 0'26\"30, but the rank display clearly shows Capless 5"; the practice card already refused to floor that (`_section_banner`'s own sentinel) and the cell did not, which was the whole of round-4 item 2. `old_clock` = delta-timed **AND** igt-bearing closer **AND** `igt_seen` (`caveats.igt_seen_in(history)`, threaded from all three views.py sites): two clocks must provably coexist in the entity's own record before the mark draws (round 17 item 2 — all-delta LBLJ wore it for comparable runs; 3 of 8 PBs marked, all noise → 0). Mutation-proved; full story in caveats.py's docstring. `grab_timed` = `Attempt.timed_at == "grab"`. SEVERITY lives here, not in the browser: one badge draws one thing, so the server picks and the client only draws — `ui/components/marks.js` holds glyph/wording/floor-rule per key and `tests/test_cross_language_parity.py` pins the two key SETS equal (order is deliberately not compared; the JS object is a lookup). Measured against a reprojected `Connection.backup` of the dev journal: of 34 current PBs, 21 `grab_timed`, 3 `old_clock`, 2 `unattributed`, 8 clean — and `grab_timed` decays on its own as new PBs land, since every star recorded from 2026-08-01 is x-cam timed. **`attempt_caveat(attempt)` is the PRACTICE LOG's own mark** (2026-08-02, reversing the earlier alarm-fatigue ruling — "I want to add an extra (!) indicator to the entry if it was technically a star grab… if you've been practicing all wrong, you should know"), shipped per row as `_attempt_json`'s `caveat`. It asks about the ROW rather than about a PB and its rank, so it is **PROVEN-only**: `timed_at == "grab"`, never the unknown `None`. Settled with a number rather than an argument — of 837 star successes in his live journal, **3 carry `"grab"` and 670 carry `None`**, so marking the unknowns would put a warning on four fifths of the log and on nothing he can act on; those stay marked on the PB badge, the surface that asserts a grade. `_proven_grab_timed` is the one predicate behind BOTH this and `pb_blocked_by`, so a row can never be marked wrong-quantity by one and offered as a legal save by the other (pinned by `tests/test_caveats.py`). `old_clock` is deliberately absent here — it is the only other key an attempt could carry alone, it would land on segment rows, and its count is unmeasured; one key is a stated scope, not a gap |
| What a practice-log row may DO about a PB (Save / Undo / nothing, and why not) | `tracking/pbaction.py` — **`pb_action(attempt, active_strat, owns_strat_pb)` is THE action-column resolver** (2026-08-20; its own module since 2026-08-22 because a caveat asks a different question): `("save"\|"undo"\|None, {reason, strat}\|None)`, combining `caveats.pb_blocked_by` (is this TIME legal) with `pb_strat_gate` (does this row belong to what I am practising, `PB_GATE_REASONS` = `foreign_strat`/`no_active_strat` — the browser draws NOTHING for either, his 2026-08-22 ruling: "the button / text just shouldn't be there"; the 2026-08-20 printed "<strat> only" chip and its `marks.js` vocabulary are gone); STRATEGY is stated before quantity since the same ruling — a foreign row draws nothing, the disabled grab-timed button included, and the quantity reason surfaces once the row is the active strategy's. Pure, so `views._attempt_json` calls it per row from the maps it holds; `TrackerService.pb_action(attempt, timer_mode)` asks it of the DATABASE (active strategy via `ActiveStrats.from_db`, ownership via `db.current_pb` on the row's own strategy) and `save_pb` requires `"save"`, `undo_pb` requires `"undo"` — so the API can never accept what the button does not offer, and a PB no ladder can grade is unreachable rather than merely marked. **The likely iterations all land here**: ungating Undo is one reordered line (`owns_strat_pb` before the gate); allowing a no-strategy save is the `no_active_strat` branch; `"undo"` is also the only statement that a row owns its strategy's PB (the `is_current_pb` flag was retired 2026-08-22 after the Rank tab misread it — match `pb.attempt_id` for the entity-wide row) |
| Which strategy an entity is being PRACTISED with (the active strategy) | `tracking/activestrat.py::ActiveStrats` — `for_star(course_id, star_id)` / `for_segment(segment_id)` / `for_attempt(attempt)`, IDENTITY only. Two ingredients, both resolved once per object: the tombstone KV (`deleted_strats`) and the Bowser reds/pipe FAMILY reject (`reds_pipe_segments`, moved here from views.py with `STAR_FAMILY_SUFFIX`/`PIPE_FAMILY_SUFFIX`). Built once per `build_session_view` / `build_route_view` and once per PB command (`from_db`). Until 2026-08-22 the rule was restated at six call sites in views.py and the route candidates applied the tombstone but never the family — `tests/test_single_source.py::test_only_activestrat_decides_which_strategy_is_ACTIVE` now forbids `masked_strat(` outside this file. `mask(strat, ek)` is the one tombstone-only reading, for the practice target, which carries its own name |
